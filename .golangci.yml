version: "2"

run:
  # The Go language version defaults to the go directive in go.mod.
  timeout: 5m
  tests: true

linters:
  default: standard
  enable:
    # correctness / safety
    - copyloopvar
    - errname
    - gochecknoinits
    - makezero
    - nilerr
    # idiomatic / modern
    - intrange
    - modernize
    - usestdlibvars
    # style / quality
    - gocritic
    - goprintffuncname
    - misspell
    - prealloc
    - revive
    - unconvert
    - unparam
    - whitespace

  settings:
    errcheck:
      exclude-functions:
        # Writing to http.ResponseWriter conventionally ignores errors.
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
    gocritic:
      disabled-checks:
        - commentFormatting   # we have our own style
        - ifElseChain
        - singleCaseSwitch
        # MarshalJSON must stay on a value receiver so that values marshaled
        # directly (not through a pointer) still merge Extra; a copy of the
        # params struct per encode is not worth losing that.
        - hugeParam
      enabled-tags:
        - diagnostic
        - performance
        - style
    misspell:
      locale: US
    prealloc:
      simple: true
      range-loops: true
      for-loops: false
    revive:
      severity: warning
      rules:
        - name: blank-imports
        - name: context-as-argument
        - name: context-keys-type
        - name: dot-imports
        - name: error-return
        - name: error-strings
        - name: error-naming
        - name: increment-decrement
        - name: range
        - name: receiver-naming
        - name: redefines-builtin-id
        - name: superfluous-else
        - name: time-naming
        - name: unexported-return
        - name: unreachable-code
        - name: var-declaration
        - name: var-naming
    staticcheck:
      # "all" enables ST/SA/QF/S checks; modernize-style suggestions ship via QF/S.
      checks:
        - all
        - -ST1000   # missing package comment - too noisy

  exclusions:
    presets:
      - std-error-handling
    rules:
      # Allow deprecated wrappers to reference deprecated symbols.
      - linters:
          - staticcheck
        text: "SA1019"
      # Example code is intentionally simplified.
      - path: examples/
        linters:
          - errcheck
          - gocritic
      # Tests get more leeway.
      - path: _test\.go
        linters:
          - errcheck
          - unparam
          - gocritic
          - revive
          - prealloc

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/s0x90/zkteco-biota-client

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
